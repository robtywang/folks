# Copy this file to .env.local and fill in your Anthropic API key.
# Without a key, the prototype will fall back to a heuristic mock parser
# (so you can still test the UI flow without paying for API calls yet).

ANTHROPIC_API_KEY=sk-ant-...
